import { ComingSoon } from "@/components/coming-soon";

/**
 * Grow tab — insights, net worth, subscriptions, budgets (wireframe flows
 * 4/6/8). Placeholder until those surfaces are built.
 */
export default function GrowScreen() {
  return <ComingSoon title="Grow" showBack={false} />;
}
