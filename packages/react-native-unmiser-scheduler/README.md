# react-native-unmiser-scheduler

Nitro Android scheduler bridge for Unmiser webhook sync.

Native owns WorkManager/AlarmManager scheduling and records due sync triggers.
TypeScript owns webhook payload delivery and cursor/log state.
